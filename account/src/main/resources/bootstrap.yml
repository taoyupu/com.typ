spring:
  application:
    name: account
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8762/eureka/,http://127.0.0.1:8763/eureka/